<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <!-- Needed to resolve whether a notes app handles CREATE_NOTE before firing it
         (Android 11+ package visibility). -->
    <queries>
        <intent>
            <action android:name="com.google.android.gms.actions.CREATE_NOTE" />
        </intent>
    </queries>

    <application
        android:name=".OneAgentArmyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OneAgentArmy">

        <!-- adjustResize so the window shrinks when the keyboard opens, which is what makes
             WindowInsets.ime report a real height to Compose. Without it the IME just draws
             on top of the layout and imePadding() has nothing to work with. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lets the system camera app write a capture into this app's cache. A
             file:// URI can't be handed to another app on modern Android, so the
             temp file is exposed through FileProvider instead.

             The authority MUST be built from applicationId, which is NOT this app's
             namespace (applicationId stayed com.piotrek.oneagentarmy so updates don't
             wipe installed data). Code side uses context.packageName, which resolves
             to the same value at runtime. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>